{
  "name": "chrome-agent-js-tests",
  "version": "1.0.0",
  "description": "jsdom suite for vendor/extract.js and the npm postinstall wrapper. Run from Rust by tests/js_suite_tests.rs, which globs *.test.js and invokes `node --test`.",
  "private": true,
  "scripts": {
    "test": "node --test *.test.js"
  },
  "license": "MIT",
  "devDependencies": {
    "jsdom": "^29.0.1"
  }
}
